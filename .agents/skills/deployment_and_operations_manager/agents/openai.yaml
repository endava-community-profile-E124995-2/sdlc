interface:
  display_name: "Deployment and Operations Manager"
  short_description: "Release planning and runtime readiness"
  default_prompt: "Use $deployment-and-operations-manager together with the shared Codex governance docs under .agents/shared and the canonical role docs under automation/roles/deployment_and_operations_manager to handle this request."

policy:
  allow_implicit_invocation: true
