interface:
  display_name: "Product and Requirements Manager"
  short_description: "Product discovery and requirements shaping"
  default_prompt: "Use $product-requirements-manager to frame this product request, choose the right artifact, tighten the requirements, and use SDLC_AUTOMATION_PROJECT_PATHS and SDLC_AUTOMATION_INFORMATION_HUB_PATHS when project-specific or information-hub context is needed."

policy:
  allow_implicit_invocation: true
