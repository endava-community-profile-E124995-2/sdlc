interface:
  display_name: "Product and Requirements Manager"
  short_description: "Product discovery and requirements shaping"
  default_prompt: "Use $product-requirements-manager to frame this request using the canonical role docs under automation/roles/product_requirements_manager."

policy:
  allow_implicit_invocation: true
