# Automation Workspace

`automation/` contains the Codex SDLC engine:

- `roles/`: canonical role packages used by the repo-local skill adapters
- `workflows/`: stage-by-stage entrypoints for intake through operations
- `templates/`: project and hub scaffolds used during onboarding
- `guides/`: reusable operator guidance for Codex usage and automation conventions
