# Fallback Notes

Fallback knowledge for this role should only be added when the active docs, structured corpus, and shared hub do not provide enough coverage.

## Rule

- Keep the legacy pack retrieval-oriented and fallback-only.
- Do not let fallback knowledge override `agent/` behavior or structured artifact contracts.
