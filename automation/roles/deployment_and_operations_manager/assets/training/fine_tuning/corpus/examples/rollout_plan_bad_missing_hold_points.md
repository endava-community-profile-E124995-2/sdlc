# Bad Example: Rollout Plan

We should deploy this today and keep an eye on things. If something goes wrong, we can always roll back. Engineering, ops, and support are all aware, so it should be fine. We will post updates if needed and move quickly so the release is not delayed.
