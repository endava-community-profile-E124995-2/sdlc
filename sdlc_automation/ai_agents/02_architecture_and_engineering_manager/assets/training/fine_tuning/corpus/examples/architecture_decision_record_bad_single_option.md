# Bad Example: Architecture Decision Record

We should split the service because microservices are cleaner.

There is no need to compare alternatives because the monolith is old.

The team will handle any impacts later.
